
// #include <stdio.h>

// #ifndef DEBUG
// #define DEBUG(...) printf(__VA_ARGS__)
// #endif

// typedef struct {
//   float x;
//   float y;
// } tocka;

// typedef struct {
//  ` tocka a;
//   tocka b;
//   tocka c;
//   tocka d;
// } cetverokut;

// int main() {
//   tocka a, b, c, d;
//   int counter = 0;

//   do {
//     scanf("%f %f %f %f %f %f %f %f", &a.x, &a.y, &b.x, &b.y, &c.x, &c.y, &d.x, &d.y);

//     counter++;
//   } while ((a.x != a.y) || (b.x != b.y) || (c.x != c.y) || (d.x != d.y));
  
//   for (int i = 0; i < counter; i++){
//     printf("test");
//   }
  
//   return 0;
// }